/*
 * Isilon SDK
 *
 * Isilon SDK - Language bindings for the OneFS API
 *
 * OpenAPI spec version: 5
 * Contact: sdk@isilon.com
 * Generated by: https://github.com/swagger-api/swagger-codegen.git
 */

use std::borrow::Borrow;
use std::rc::Rc;

use futures;
use futures::{Future, Stream};
use hyper;
use serde_json;

use super::{configuration, Error};

pub struct QuotaReportsApiClient<C: hyper::client::Connect> {
    configuration: Rc<configuration::Configuration<C>>,
}

impl<C: hyper::client::Connect> QuotaReportsApiClient<C> {
    pub fn new(configuration: Rc<configuration::Configuration<C>>) -> QuotaReportsApiClient<C> {
        QuotaReportsApiClient {
            configuration: configuration,
        }
    }
}

pub trait QuotaReportsApi {
    fn get_report_about(
        &self,
        rid: &str,
    ) -> Box<Future<Item = ::models::ReportAbout, Error = Error>>;
}

impl<C: hyper::client::Connect> QuotaReportsApi for QuotaReportsApiClient<C> {
    fn get_report_about(
        &self,
        rid: &str,
    ) -> Box<Future<Item = ::models::ReportAbout, Error = Error>> {
        let configuration: &configuration::Configuration<C> = self.configuration.borrow();

        let method = hyper::Method::Get;

        let uri_str = format!(
            "{}/platform/1/quota/reports/{Rid}/about",
            configuration.base_path,
            Rid = rid
        );

        let uri = uri_str.parse();
        // TODO(farcaller): handle error
        // if let Err(e) = uri {
        //     return Box::new(futures::future::err(e));
        // }
        let mut req = hyper::Request::new(method, uri.unwrap());
        configuration.set_session(&mut req).unwrap();

        // send request
        Box::new(
            configuration
                .client
                .request(req)
                .and_then(|res| res.body().concat2())
                .map_err(|e| Error::from(e))
                .and_then(|body| {
                    let parsed: Result<::models::ReportAbout, _> = serde_json::from_slice(&body);
                    parsed.map_err(|e| Error::from(e))
                })
                .map_err(|e| Error::from(e)),
        )
    }
}
