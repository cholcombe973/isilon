/*
 * Isilon SDK
 *
 * Isilon SDK - Language bindings for the OneFS API
 *
 * OpenAPI spec version: 5
 * Contact: sdk@isilon.com
 * Generated by: https://github.com/swagger-api/swagger-codegen.git
 */

use std::borrow::Borrow;
use std::rc::Rc;

use futures;
use futures::{Future, Stream};
use hyper;
use serde_json;

use super::{configuration, Error};

pub struct RemotesupportApiClient<C: hyper::client::Connect> {
    configuration: Rc<configuration::Configuration<C>>,
}

impl<C: hyper::client::Connect> RemotesupportApiClient<C> {
    pub fn new(configuration: Rc<configuration::Configuration<C>>) -> RemotesupportApiClient<C> {
        RemotesupportApiClient {
            configuration: configuration,
        }
    }
}

pub trait RemotesupportApi {
    fn get_remotesupport_connectemc(
        &self,
    ) -> Box<Future<Item = ::models::RemotesupportConnectemc, Error = Error>>;
    fn update_remotesupport_connectemc(
        &self,
        remotesupport_connectemc: ::models::RemotesupportConnectemcConnectemc,
    ) -> Box<Future<Item = (), Error = Error>>;
}

impl<C: hyper::client::Connect> RemotesupportApi for RemotesupportApiClient<C> {
    fn get_remotesupport_connectemc(
        &self,
    ) -> Box<Future<Item = ::models::RemotesupportConnectemc, Error = Error>> {
        let configuration: &configuration::Configuration<C> = self.configuration.borrow();

        let method = hyper::Method::Get;

        let uri_str = format!(
            "{}/platform/1/remotesupport/connectemc",
            configuration.base_path
        );

        let uri = uri_str.parse();
        // TODO(farcaller): handle error
        // if let Err(e) = uri {
        //     return Box::new(futures::future::err(e));
        // }
        let mut req = hyper::Request::new(method, uri.unwrap());
        configuration.set_session(&mut req).unwrap();

        // send request
        Box::new(
            configuration
                .client
                .request(req)
                .and_then(|res| res.body().concat2())
                .map_err(|e| Error::from(e))
                .and_then(|body| {
                    let parsed: Result<::models::RemotesupportConnectemc, _> =
                        serde_json::from_slice(&body);
                    parsed.map_err(|e| Error::from(e))
                })
                .map_err(|e| Error::from(e)),
        )
    }

    fn update_remotesupport_connectemc(
        &self,
        remotesupport_connectemc: ::models::RemotesupportConnectemcConnectemc,
    ) -> Box<Future<Item = (), Error = Error>> {
        let configuration: &configuration::Configuration<C> = self.configuration.borrow();

        let method = hyper::Method::Put;

        let uri_str = format!(
            "{}/platform/1/remotesupport/connectemc",
            configuration.base_path
        );

        let uri = uri_str.parse();
        // TODO(farcaller): handle error
        // if let Err(e) = uri {
        //     return Box::new(futures::future::err(e));
        // }
        let mut req = hyper::Request::new(method, uri.unwrap());
        configuration.set_session(&mut req).unwrap();

        let serialized = serde_json::to_string(&remotesupport_connectemc).unwrap();
        req.headers_mut().set(hyper::header::ContentType::json());
        req.headers_mut()
            .set(hyper::header::ContentLength(serialized.len() as u64));
        req.set_body(serialized);

        // send request
        Box::new(
            configuration
                .client
                .request(req)
                .and_then(|res| res.body().concat2())
                .map_err(|e| Error::from(e))
                .and_then(|_| futures::future::ok(())),
        )
    }
}
